b'blob 1609\x00lst=[]\nlst="".join([letter.upper() for letter in "hello"])\nprint(lst)\nlst=[i**2 for i in range(5) if i % 2 == 0]\nprint(lst)\n\nlst.pop(0)\nprint(lst)\n\ndef noletter1(wordlist,letter=\'z\'):\n    for word in wordlist:\n        if letter in word:\n            return False\n    return True\n\ndef noletter2(wordlist,letter=\'z\'):\n    noz=True\n    for word in wordlist:\n        if letter in word:\n            noz=False\n    return noz\n\nwordlist=[\'zizzer\']+[\'aardvark\']*10000000\nprint(noletter1(wordlist))\nprint(noletter2(wordlist))\n\ndef freq_letter(string):\n    count={}\n    for char in string:\n        if char in count.keys():\n            count[char]+=1\n        else:\n            count[char]=1\n    count2=[]\n\n    #sorting by number\n    #for key in count.keys():\n    #    lst=(count[key],key)\n    #    count2.append(lst)\n    #count2=sorted(count2)\n\n    for key in count.keys():\n        lst=(key,count[key])\n        count2.append(lst)\n    \n    #sorting by number and alphabet\n    max_freq=0\n    max_char=\'\'\n    for a in range(0,len(count2)):\n        if count2[a][1]>=max_freq:\n            if max_char:\n                if count2[a][1]<max_char:\n                    max_char=count2[a][1]\n                    max_freq=count2[a][0]\n            else:\n                max_char=count2[a][1]\n                max_freq=count2[a][0]\n            \n    print((max_char,max_freq))\n    #print((countsorted[-1][1],countsorted[-1][0]))\n    \n\nprint("ANSWER")\nfreq_letter(\'aabbc\')\nfreq_letter(\'wooloomooloo\')\nfreq_letter(\'abacuses\')\n\n#freq_letter0(\'aardvark\')\n\nfruit_prices={"apple":0.5, "banana":19, "durian":7}\nprint(sorted(fruit_prices))\n\n    \n'